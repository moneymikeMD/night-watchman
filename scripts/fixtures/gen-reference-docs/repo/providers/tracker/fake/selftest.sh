#!/bin/bash
#
# selftest.sh — must NOT appear in generated scripts.mdx; proves the
# provider-selftest exclusion (bare "selftest.sh", not "*-selftest.sh").

echo "selftest fixture, should be excluded"

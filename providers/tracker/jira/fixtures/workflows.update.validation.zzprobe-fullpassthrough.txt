# ./jira-api.sh --yes write POST /workflows/update/validation '<envelope>'
# Captured live 2026-09-11 against example.atlassian.net, the orphan
# workflow "Software Simplified Workflow for Project ZZPROBE" (project
# ZZPROBE itself was deleted before this session — see
# workflows.bulkget.zzprobe-secrets.txt's header). This is ROUND-3 REVIEW
# ITEM 7: the FULL-PASSTHROUGH body build_update_body now renders
# (existing statuses/transitions carried forward with EVERY field the
# --show-secrets bulk-get returned — actions/validators/triggers/links/
# properties/description all intact, real ruleKey/permissionKey values
# included) plus the six target statuses/transitions added. Non-mutating;
# /workflows/update itself was NEVER issued against ZZPROBE or any project
# in this session. HTTP 200, `{"errors": []}` — ZERO errors, ZERO
# warnings, confirming the full-passthrough shape needs no field dropped
# to validate (unlike the earlier minimal-reshape design this superseded,
# which validated fine but silently stripped rules on write — see
# build_update_body's header for why validation success alone was never
# sufficient evidence of a safe write).
HTTP 200
{
  "errors": []
}

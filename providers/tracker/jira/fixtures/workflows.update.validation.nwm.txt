# ./jira-api.sh --yes write POST /workflows/update/validation '<envelope>'
# Captured live 2026-09-11 against example.atlassian.net, project NWM,
# using the IDENTICAL (min) body shape as
# workflows.update.validation.spk4.txt's final iteration (workflow
# id/version substituted for NWM's own) — see that fixture's header for
# the full iteration history that led here. Confirms the shape is not
# project-specific. Non-mutating; /workflows/update itself was never
# issued against NWM or any project in this session. No redaction
# applied (no credential-shaped field present). Nothing else trimmed.
HTTP 200
{
  "errors": []
}

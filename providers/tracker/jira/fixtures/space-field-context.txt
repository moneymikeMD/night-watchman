# space-field-context.txt — GET /rest/api/3/field/customfield_10044/context
# response: one default (global) context, the shape a freshly-created
# custom field gets.
#
# PROVENANCE: authored, not captured live — see
# space-project-classic.txt's header.
HTTP 200
{"maxResults": 50, "startAt": 0, "total": 1, "isLast": true,
 "values": [{"id": "10200", "name": "Default Configuration Scheme for executor", "isGlobalContext": true}]}

# Session handoff — 2026-09-19

Read once, then move to `docs/handoffs/archive/`. Tickets and
`docs/decisions.md` are ground truth; this is a pointer.

## What changed

- **Five tickets landed**: NWM-113, NWM-127, NWM-120, NWM-134, NWM-123.
  v0.9.0 and v0.9.1 both released.
- **comment-lint now comes from the public `ai-toolkit`** (NWM-126, landed at
  the start of the day) rather than a script shipped here. The job keeps its
  name so the required check still resolves.
- **A worker's exit is now a real handoff.** `land-branch.sh` writes the
  worker's closing state to the tracker and reads it back before ending the
  session (NWM-120), gated on an actual hand-off file rather than on
  `HERDR_ENV` alone (NWM-134), with `scripts/land-ack.sh` as the consumer.
- **The guard hook matches the resolved binary**, so `/usr/bin/git`, a
  PATH-shadowing shim and a renamed symlink are all caught (NWM-123) — while
  resolution is confined to command position, so an argument that merely
  shares a name with a binary is not.
- **`land-branch.sh` splits** — decided and owner-confirmed (NWM-127). A
  generic merge-and-push core goes to `ai-toolkit`; the lifecycle, both
  trackers and the herdr teardown stay here as a wrapper. The extraction is
  NWM-131.

## Verified, not assumed

Observed, not inferred:

- The closing-state round trip completed against the **live orchestrator
  pane**, not a stub: landing NWM-134 notified `w5C:p1`, the orchestrator read
  the prompt and ran `land-ack.sh`, which validated the `nw-ack-` prefix and
  created the file. The bounded degrade also demonstrated itself unplanned —
  land-branch gave up at 10s and completed anyway, because the ack arrived
  late. Both tickets' last inferred claim is now measured.
- The guard's argument-position regression was reproduced against `main` and
  re-checked after the fix: `echo hello g stash` exits 0 as before, while the
  same shim as a command head exits 2.
- NWM-123's ticket body was **wrong about one of its two defects**. The
  in-worktree false positive does not exist — `git_target_is_linked_worktree`
  already exempted linked worktrees. Verified against the pre-change guard.
  That clause is recorded NOT MET deliberately.
- Suites at landing: guard 118/118, land-branch 35, land-branch-jira 16,
  land-ack 9/9. comment-lint clean against the **pinned** `@v1` linter.
- CI green on `main` at `300b85d`.

Assumptions, flagged:

- NWM-134's live ack was exercised by a bash loop in a scratch pane *and*,
  separately, by the real orchestrator during NWM-134's own landing. The
  second is the stronger evidence and is what closes the clause.
- `memorygraph link` is reported (by a worker, first-hand, not re-tested
  here) to reject `RELATES_TO` and `SUPERSEDES` while accepting `SOLVES`,
  `CAUSES`, `CONTRADICTS`, printing an internal error rather than the valid
  set. If true, CLAUDE.md's "supersede the old memory" instruction has no
  mechanism behind it. Read a link back rather than trusting the success line.

## What remains

Open: NWM-119, 122, 124, 128, 129, 130, 131, 132, 133, 135, 136, 137, 138, 139.
`issues.py lint` is clean, so `issues.py waves` can be trusted.

Seven of those were filed today because something **passed a gate and was still
broken**. That is the session's theme and the reason the tooling tickets
exist: NWM-132 (a contract written where the tool does not read it), NWM-133
(an external blocker with nowhere structured to live), NWM-135 (a review that
only checks what its brief enumerated), NWM-136 (secrets in command output),
NWM-137 (a selftest that under-reports its own failures), NWM-138 (a
read-back with no retry), NWM-139 (a generated-docs check that CI never
runs — found while writing this note, because NWM-134 shipped a new script
without its reference page and three required checks plus a spec-review all
passed anyway).

Context a ticket body does not carry:

- **NWM-128 is not startable despite what the tool says.** Its blocker is
  prose, so `next` and `waves` both list it. `ai-toolkit@v1` publishes only
  `actions/comment-lint`; this needs a known-issue action and a release
  there first. NWM-133 is the fix for the class.
- **NWM-131 now waits on NWM-138 as well**, because a false read-back failure
  would be inherited by the extracted core.

## Decisions only the owner can make

1. **Rotate the exposed credentials.** A worker printed
   `OP_SERVICE_ACCOUNT_TOKEN` and `MEMORY_FALKORDB_PASSWORD` in full via
   `env | grep`. All real values were scrubbed from every transcript and
   verified at zero, and a separate older `ops_` token found in a homelab
   transcript was scrubbed too. The owner chose **scrub only, no rotation**.
   The op service account token is still live and was printed; rotation
   remains the stronger option and is recorded as declined, not forgotten.
2. **The installed plugin is behind again.** Installed is 0.9.0; `main` is
   0.9.1 after release-please merged #10. This is the same gap that hid
   NWM-118's guard fix all of yesterday — `Completed` and released is not
   deployed, because the hooks that run come from the plugin cache. Update it
   and re-check.

## Suggested next

Load `session-start`. Wave 1 is NWM-122, 132, 135, 136, 138 and 139 in
parallel — `issues.py waves` says so and their paths are disjoint. NWM-128 is
deliberately not in it: it is chained behind NWM-139 (both edit `ci.yml`) and
its real blocker is external anyway, so do not dispatch it.

Two process rules were adopted mid-session and are worth keeping:

- Every spec-review brief must require running the repo's **required checks**
  unconditionally, not only the concerns the brief names. It caught two
  branches in its first wave. NWM-135 makes it structural.
- A feature spanning two actors needs at least one test crossing the seam in
  the real environment. The closing-state handoff passed two verifies and two
  spec-reviews while being unable to complete end to end, because every test
  built its own fixture instead of letting a live worker write a real file
  into a real worktree.

Cost for this session is in `.night-watchman/last-session-cost.txt`; during a
wave that file is written by whichever session ends last, so it may be a
worker's number rather than the orchestrator's.

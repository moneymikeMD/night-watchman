# Session handoff — 2026-09-18

Read once, then move to `docs/handoffs/archive/`. Tickets and
`docs/decisions.md` are ground truth; this is a pointer.

## What changed

- **Three tickets landed and v0.8.0 is released.** NWM-121 (README claimed the
  docs site was unpublished while linking it), NWM-118 (the guard hook's
  re-entrancy bypass — `bash -c "true" <destructive>` was allowed), NWM-112
  (`dispatch start` now hands agents a complete brief, so the orchestrator
  stops retyping standing orders after every start).
- **The repo's GitHub settings were hardened.** Both branch rulesets had been
  targeting `refs/heads/protect_main`, a branch that has never existed here —
  `main` was force-pushable and deletable for three days while the settings
  page showed two active rulesets. Now on `~DEFAULT_BRANCH`. Secret scanning,
  push protection and Dependabot security updates were all off; all on.
- **CI exists for the first time**, on macOS because this repo targets bash
  3.2. Three required checks: `selftests`, `docs-site`, `comment-lint`.
- **Versioning moved to release-please**, seeded at 0.7.2 and cut 0.8.0.
  `scripts/release.sh` now overlaps it — NWM-124 retires it, gated on that
  first release having happened, which it now has.
- **Comment audit**: 9,002 → 3,706 comment lines (25% → 12% of the codebase),
  74 section dividers, five essay-length file headers. ~60 findings were
  written to memory-graph before their comments were deleted. Enforced going
  forward by `scripts/comment-lint.py` as a required check.

Full reasoning for the hardening and the comment rule is in
`docs/decisions.md`, dated 2026-09-18.

## Verified, not assumed

Everything below was observed, not inferred:

- The NWM-118 bypass reproduced live before the fix (exit 0) and exits 2
  after; the hook emits zero stderr on a plain allow.
- 33 selftests run; 2 fail. Both failed on `main` before this session and fail
  identically now — `providers/config-selftest.sh` and
  `jira-workflow-apply-selftest.sh`, each one assertion, each filed in
  `docs/known-issues/`.
- Every comment-audit diff is comment-only: both versions of each file were
  compared with comments stripped, and no executable line changed.
- CI is green on `main` at `89e64b0`; `gen-reference-docs.sh --check` passes
  (it was already failing on `main` before this session began).

Assumptions, flagged as such:

- `comment-lint.py`'s heredoc handling is a heuristic, not a shell parser. It
  was wrong once today in a way that reported *clean* on 450 unread lines;
  that specific shape is fixed and pinned by a fixture, but other shapes may
  exist.
- NWM-112's verify clause "one real dispatch starts without a second prompt"
  is **NOT MET** and was landed anyway, recorded on the ticket. The next wave
  exercises it as a side effect.

## What remains

Sprint 76: NWM-113, NWM-119, NWM-120, NWM-122, NWM-124, plus epic NWM-125
(E6, extracting generic tooling to a public `ai-toolkit` repo) with children
NWM-126 through NWM-130. All are chained with `Blocks` links where they share
`touches` paths — `issues.py lint` is clean apart from NWM-123 below, so
`issues.py waves` can be trusted to sequence them.

Two pieces of context a ticket body does not carry:

- **NWM-127 is deliberately a decision, not a move.** `land-branch.sh` is on
  the owner's migration list but encodes this project's ticket lifecycle plus
  the tracker and dispatch provider seams. It must be classified before any
  code moves, and it does not block the other four.
- **NWM-129 and NWM-119 both touch `claude-cost.py`.** Linked, but the
  sequencing decision (land NWM-119 here first, or migrate then do it in
  ai-toolkit) has not been made.

## Decisions only the owner can make

1. **NWM-123** — filed by the owner, has no `verify` and no `executor`, so it
   is excluded from `next` and every wave and can never be dispatched as
   written. Its subject is real: the guard hook blocks `git` inside an agent's
   own worktree and is bypassable with `/usr/bin/git`.
2. **Who creates `moneymikeMD/ai-toolkit`.** The homelab session holds LAB-273
   for it. This session declined to create it on a peer's relay — a new public
   repo publishes a name and is awkward to undo, and authorisation never came
   from the owner directly. One line from him settles it; the seed is
   `89e64b0`.
3. **Four open Dependabot PRs**, all major bumps, so auto-merge will not touch
   them. #1 bumps `release-please-action` v4 → v5, which started working today.

## Suggested next

Load `session-start`. It will preflight the providers and fan the sprint out;
`issues.py waves` already reflects the chaining above. If the next session is
picking up E6 rather than the sprint, read `docs/decisions.md`'s 2026-09-18
entries first — the boundary rule for what belongs in `ai-toolkit` versus the
private dotfiles repo is recorded there, and applying it wrongly is how the
split gets re-litigated.

Cost for this session is in `.night-watchman/last-session-cost.txt`, but note
that file is written by whichever Claude session ends last — during a wave it
will be a worker's number, not the orchestrator's.

---
title: Ethos seed
description: Generated from templates/ethos.md, reproduced verbatim.
---
{/* GENERATED by scripts/gen-reference-docs.sh — do not hand-edit */}

# Ethos — how the owner decides

*Template. Copy this into your own repo as `docs/ethos.md` (or wherever your
decision log lives) and replace the example row with your own. Delete this
header comment once you do.*

A profile of the owner's (or user's) decision preferences, built from the
questions that have actually been asked and how they were answered. The
point is to ask fewer questions over time: before asking a question the
owner has plausibly already answered, check here; if a default below
covers the decision, apply it, say which default you applied, and move on
without asking.

Before asking a "which approach" or "what should this do" question,
classify it first: if the answer is a fact you could observe by running
something (behavior, timing, output, whether an eval separates), it is not
the owner's to answer — go observe it instead of asking. Reserve questions
for a genuine preference or product call no experiment can settle.
Ported from pstack poteto-mode, 2026-09-14.

Every entry cites the decision it was inferred from. An entry with no
evidence is a guess and does not belong here. When a new answer contradicts
an existing default, **the default is wrong** — fix it in place, do not add
an exception clause on top of it.

Maintained by whoever asks the question: after every owner answer that
either confirms or moves a default, append a log row (or edit the existing
default's evidence) before the session ends.

## Defaults that can be assumed

| Default | Evidence |
| --- | --- |
| **Ship the smaller, reversible option first.** When two designs solve the same problem and one of them is trivially undoable, do that one now and treat the larger option as a follow-up ticket only if the first proves insufficient — don't propose the bigger design as the first move just because it's "more correct." | 2026-08-01: chose a feature-flagged rollout over a full migration for the same underlying change; the flag was removed within a week once the smaller version was confirmed sufficient, and the full migration was never needed. |

## Still ask

Categories that stay a question regardless of how many defaults exist —
start with these and add your own as they come up:

- Anything that deletes data with no backup or undo path.
- Spending money, or opening an account with an external provider.
- Rotating a credential the owner/user manages by hand, where the system
  doing the rotating can't also update the place that credential is used.
- Changing what a system is fundamentally *for* — repurposing a host,
  moving a service's role, anything that changes the answer to "what is
  this thing" rather than "how does this thing work."

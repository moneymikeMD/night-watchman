version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly

  # Bun is its own ecosystem, not a variant of npm; needs the text bun.lock.
  - package-ecosystem: bun
    directory: /docs/preview/website
    schedule:
      interval: weekly
